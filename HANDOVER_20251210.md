# Clip Composer 引き継ぎドキュメント

**作成日:** 2025-12-10
**最新コミット:** f1a89fe (feat: ライセンス認証システム実装 & DMGビルド設定)

---

## 📊 プロジェクト概要

**Clip Composer** - SNSショート動画を大量生成するデスクトップアプリ

- **技術スタック:** Electron + React + Python (MoviePy/FFmpeg)
- **対象OS:** macOS (arm64)
- **状態:** 開発中（配布準備段階）

---

## ✅ 完了済み機能

### コア機能
- タイムラインUI（V1/V2/S1/S2レイヤー）
- ランダムレイヤー（動画/画像対応）
- テキストレイヤー / 可変テキストレイヤー
- 調整レイヤー（エフェクト）
- BGM/SE音声レイヤー
- プロジェクト保存/読み込み
- 自動保存/テンプレート機能
- CSV一括書き出し
- 並列動画生成
- プレビュー再生

### ライセンス認証システム（クライアント側）
| ファイル | 説明 |
|----------|------|
| `frontend/src/license/LicenseManager.cjs` | コア認証モジュール |
| `frontend/src/renderer/components/LicenseDialog/` | ライセンス入力UI |
| `frontend/src/renderer/pages/LicensePage.jsx` | ライセンス画面 |

**機能:**
- デバイスIDバインド（ハードウェア紐付け）
- オフライン猶予期間（72時間）
- 起動時ライセンスチェック
- activate / verify / deactivate API

**セキュリティ対策:**
- デバイスIDバインド強化（license.jsonコピー攻撃対策）
- IPC認証バイパス防止

---

## 🚧 未完了・保留タスク

### 1. ライセンス認証サーバー連携（保留）

**状態:** クライアント側完了、サーバー連携は販売前に対応

**必要な作業:**
1. サーバー側APIエンドポイント実装
   - `POST /v1/license/activate`
   - `POST /v1/license/verify`
   - `POST /v1/license/deactivate`
2. JWT署名検証の有効化
   - `LicenseManager.cjs` の `verifyJwtSignature()` 有効化
   - 公開鍵埋め込み
3. サーバーURL設定
   - 現在: `https://api.clipcomposer.app`（プレースホルダー）
   - 予定: `sd-create.jp` のサブドメイン

**開発時のスキップ方法:**
```bash
SKIP_LICENSE_CHECK=true npm start
```

---

### 2. 配布準備（次回対応）

| タスク | 状態 | 備考 |
|--------|------|------|
| アプリアイコン作成 | ❌ 未作成 | icon.icns (1024x1024px) |
| DMGビルド | ⏳ 準備完了 | `npm run dist:mac` |
| Apple公証 | ❌ 未対応 | Developer Account必要 |

**アイコン要件:**
- `assets/icon.icns` - macOS用（1024x1024px）
- `assets/icon.ico` - Windows用（256x256px）
- `assets/icon.png` - 汎用（512x512px）

**ビルドコマンド:**
```bash
cd /Users/shoeigoto/Desktop/clip-composer
npm run dist:mac
# 出力: dist/Clip Composer-x.x.x-mac-arm64.dmg
```

---

## 📁 重要ファイル

```
clip-composer/
├── frontend/
│   └── src/
│       ├── main.cjs              # Electronメインプロセス
│       ├── preload.cjs           # IPC通信設定
│       ├── license/
│       │   └── LicenseManager.cjs # ライセンス管理
│       └── renderer/
│           ├── components/
│           │   └── LicenseDialog/ # ライセンスUI
│           └── pages/
│               └── LicensePage.jsx
├── backend/
│   ├── main.py                   # Pythonエントリーポイント
│   └── modules/
│       ├── video_processor.py
│       ├── parallel_processor.py
│       └── effects/
│           └── ffmpeg_filters.py
├── assets/
│   ├── entitlements.mac.plist    # macOS権限設定
│   └── icons/                    # アイコン（要作成）
├── electron.config.js            # ビルド設定
└── HANDOVER_LICENSE_SYSTEM.md    # ライセンスシステム詳細仕様
```

---

## 🔧 開発環境

```bash
# 依存関係インストール
npm install
cd backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt

# 開発サーバー起動（ライセンスチェックスキップ）
SKIP_LICENSE_CHECK=true npm start

# 通常起動（ライセンスチェックあり）
npm start
```

---

## 📋 次回作業の優先順位

1. **アプリアイコン作成**
   - 1024x1024pxのPNG画像を用意
   - `.icns` / `.ico` / `.png` に変換

2. **DMGビルド・動作確認**
   - `npm run dist:mac` 実行
   - 生成されたDMGをインストールしてテスト

3. **（オプション）Apple公証**
   - Developer Account取得（¥13,000/年）
   - Notarization対応

4. **（販売前）ライセンスサーバー連携**
   - サーバーAPI実装
   - JWT署名検証有効化

---

## 🔑 関連情報

### ConoHa VPS（ライセンスサーバー予定地）
- **IP:** 163.44.114.42
- **ドメイン:** sd-create.jp
- **SSH:** `ssh root@163.44.114.42`
- **既存アプリ:** Laravel コーポレートサイト

### Git
- **ブランチ:** develop
- **最新コミット:** f1a89fe

---

*作成: Claude Code (ディレクター)*
